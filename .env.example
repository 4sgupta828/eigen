# Eigen deployment config (single vertical per deployment)
EIGEN_ACTIVE_VERTICAL=medical
EIGEN_PROVIDER_MODE=replay          # replay (free) | record | live
EIGEN_LLM_MODEL=claude-sonnet-5
EIGEN_CASSETTE_ROOT=evals/cassettes
EIGEN_CORPUS_DSN=postgresql://user:pass@localhost:5432/eigen
# Secrets (record/live only — never commit real values):
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
TAVILY_API_KEY=
