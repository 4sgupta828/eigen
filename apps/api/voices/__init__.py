"""Voices — first-person startup content (founder/investor essays and podcast chapter pointers).

The corpus lives in the kernel's `rs_block` table like every other source; this package is only the
mode on top of it: search shaped as moments, the gated binding of a guest to a company we index, and
the routes. See docs/specs/voices.md.
"""
